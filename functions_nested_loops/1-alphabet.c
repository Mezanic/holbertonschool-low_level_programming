#include"main.h"

/**
 * main - hat prints the alphabet, in lowercase
 *
 * Return: 0
 */

int main(void)
{
	int alph;

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		_putchar(alph);

	}
	_putchar('\n');
	return (0);
}
